# HospitalManagementSystem
A Java Swing application for managing hospital beds, patients, and appointments. Features full CRUD operations, input validation, foreign key error handling, and a live bar chart dashboard showing appointments per doctor - built with Apache Derby and NetBeans.
